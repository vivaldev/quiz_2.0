import React from "react";
import { Heading } from "@chakra-ui/react";

const StartGame = (props) => {
  return (
    <>
      <Heading>Are you ready to start the Quiz, {state.username} </Heading>
    </>
  );
};

export default StartGame;
